import UFeedback from "./feedback.vue";
import { withInstall } from "../../util";

export type UserInfo = {
  id: number;
  avatar: string;
  nickname: string;
  userType: number;
};

export { UFeedback };
